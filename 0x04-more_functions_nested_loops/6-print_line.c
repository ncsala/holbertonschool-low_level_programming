#include "holberton.h"

/**
 * print_diagonal (int n)
 * Return: void
 */

void print_diagonal(int n)
{
	int r = 0, s = 0;

 	if (n <= 0)
		_putchar('\n');

	while (n > 0)
	{
		while (s  > 0)
		{
			_putchar(' ');
			i = i - 1;
		}	
	}
}
